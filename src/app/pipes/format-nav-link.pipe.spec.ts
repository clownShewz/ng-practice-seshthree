import { FormatNavLinkPipe } from './format-nav-link.pipe';

describe('FormatNavLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNavLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
